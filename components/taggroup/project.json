{
	"$schema": "../../node_modules/nx/schemas/project-schema.json",
	"name": "taggroup",
	"tags": ["component", "legacy"],
	"targets": {
		"profile": {},
		"build": {
			"defaultConfiguration": "legacy"
		},
		"test": {
			"defaultConfiguration": "scoped"
		},
		"lint": {
			"options": {
				"cwd": "components/taggroup"
			}
		},
		"clean": {
			"options": {
				"cwd": "components/taggroup"
			}
		},
		"watch": {}
	}
}
